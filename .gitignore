__pycache__/
*.pyc
.pytest_cache/
.venv/

# Downloaded on first run by nazrah.gaze_tracker.ensure_model()
nazrah/models/

usage_log.csv

# Saved by nazrah.calibration.save_calibration() — specific to one
# camera's position/angle and one screen's resolution, not something to
# commit.
calibration_data.json
