# These run on the Pi (Linux) — a CRLF line ending breaks the shebang line
# in a .sh script outright ("bad interpreter" errors) and can confuse
# .desktop file parsing, so force LF regardless of local git config.
*.sh text eol=lf
*.desktop text eol=lf
